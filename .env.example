# Copy to .env and fill in.
# Only needed when the UI is NOT served from the same host as the API.
# In dev (npm run dev) the Vite proxy handles routing — leave this empty.
# In production (built static files served separately), set the full API origin:
VITE_API_URL=http://<truenas-ip>:8000
